#spring:
#  datasource:
#    driver-class-name: org.postgresql.Driver
#    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5433}/${POSTGRES_DB:hospital_db}
#    username: ${POSTGRES_USER:postgres}
#    password: ${POSTGRES_PASSWORD:1234}